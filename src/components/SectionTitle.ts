import styled from 'styled-components'
import { font } from '../styles/Common'

export const SectionTitle = styled.h2`
	${font({ weight: 700, lineHeight: 1.2, Fmax: 48, Fmin: 38 })}
	letter-spacing: 0%;
	text-align: center;
	margin-bottom: 80px;
`
